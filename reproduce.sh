#!/usr/bin/env bash
# reproduce.sh — one-shot reproduction of every figure from raw inputs.
# Built up over the project. By submission it should:
#   1. extract raw_blocks.parquet from BigQuery
#   2. run the attribution matrix
#   3. compute metrics
#   4. regenerate every figure in figures/
set -euo pipefail
echo "reproduce.sh: not yet implemented — fill in as the pipeline grows."
